# Copy to .env and fill in. NEVER commit .env.
# Go-live switch: point the proxy at the real Orbio gateway instead of the mock.
ORBIO_UPSTREAM=https://api.orbio.so/api
ORBIO_KEY=sk-orbio-...        # your minted key; kept out of git by .gitignore
